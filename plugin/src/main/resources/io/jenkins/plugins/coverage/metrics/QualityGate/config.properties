title.threshold=Threshold
description.threshold=If the actual coverage (or metric) is less than this threshold \
    then a build is considered as unstable or failed, respectively.

title.baseline=Baseline
description.baseline=Select the baseline that should be used when the coverage metric will be evaluated.

title.metric=Metric
description.metric=Select the coverage metric that should be evaluated for this quality gate.

title.warning=Result
description.warning=Determines if the build should be set to unstable or failed if the quality gate is not passed.
